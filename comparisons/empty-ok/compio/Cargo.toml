[package]
name = "compio-empty-ok"
version = "0.1.0"
edition = "2021"

[dependencies]
# Lightweight hyper-style example on compio runtime when available.
# Path override: see ../../.cargo/config.toml.example
compio = { version = "0.13", features = ["runtime", "macros", "io"] }
compio-net = "0.5"
compio-dispatcher = "0.5"
bytes = "1"
http-body-util = "0.1"
http = "1"
