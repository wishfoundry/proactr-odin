# qpack

QPACK (RFC 9204) field compression for HTTP/3.

**Provenance:** copied from vapor-http (`odin-http` tree) into proactr-odin for the HTTP/3 + client track.
Depends only on package `huffman` (sibling) and Odin core.
