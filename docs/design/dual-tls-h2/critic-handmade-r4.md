# Handmade / Craft Design Critique — R4 (Plan A only)

**Critic posture:** Casey Muratori / data-oriented / Odin house style.  
**Bar:** WOW ≥ 9. No participation trophies. Graft wins only if they *carve*, not if they *accumulate*.  
**Subject:** Plan A R4 graft — B residual wins absorbed; spine remains A (`plan-a.md`).  
**Prior craft:** R2 A **9.25** WOWED · R3 spotcheck A **9.35** WOWED · R2 residual craft nits (PC diet, density, Exec_Op laundry).  
**Not in scope:** Plan B re-score (B already WOW’d; this is graft integrity on A).

**Question asked of every type, phase, and graft row:**  
Would a skilled engineer, alone at a whiteboard, invent *this* cut — or is it interface soup, vapor vocabulary with the serial numbers filed off, dual-maint forever, or a portfolio merge that forgot which spine it serves?

**R4-specific kill tests (user mandate):**

1. Graft must **not** introduce vapor **dual-maint forever**.  
2. Graft must **not** introduce **vtable soup**.  
3. **Steal-vs-own** must be normative, not a slogan.  
4. **Session apply** = private switch; **no `Session_Wire` product**.

---

## Verdict headline

| Plan | Overall | WOWED | vs R3 craft |
|------|---------|-------|-------------|
| **A (R4)** | **9.45 / 10** | **Yes** | 9.35 → **9.45**; graft closed dual-maint / vtable attack surface without diluting Slot/Pipe, PT1 must-alias, Seal_Unit, or 4× HW firehose CI |

**One-line judgment:** R4 is a **controlled absorption** of B’s honesty packaging and close/port law into A’s carved spine — not a dual-world merge and not a Provider museum. Steal-vs-own and private `framer.kind` apply are freeze-grade. Residual craft debt is **document density** and two private ghosts (`Host_Pull` proc, Exec_Op laundry length) — not ontology failure.

---

## Mandate verification (R4 graft integrity)

| Mandate | Evidence in plan | Status |
|---------|------------------|--------|
| **No dual-maint forever** | §A.4 steal/own table; §G “ONE owner in this tree”; Phase 3 “Port … into proactr types (not forever-vendored vapor server)”; D27; anti-pattern “Forever dual-maint vapor package fork”; graft log “Not reopened: dual Loop worlds … dual H1/H2 host architectures” | **PASS — not introduced** |
| **No vtable soup** | §A.1 “No Adapter/Session_Wire/Provider product surfaces”; §H.4 “private host switch, not a product `Session_Wire` struct of function pointers”; D28; App NEVER list; graft log “Session_Wire product type” not reintroduced | **PASS — not introduced** |
| **Steal vs own** | §A.4 four-row table (facts vs types); §G engine port boundary; D27; F.1 “peer-measured defaults … not a package import” | **PASS — normative** |
| **Session apply private switch** | §H.4 `session_apply_write` sketch: switch on `conn.framer.kind`; not a plug-in type | **PASS** |
| **No Session_Wire product** | Same as vtable + D28 + NEVER + anti-patterns | **PASS — hard ban** |
| **A spine not diluted** | Graft log “kept A residuals”: Conn_Pt_Ring must-alias; firehose CI ≳4× HW; typed Seal_Unit+seal_q+rr_cursor+gen; allocator §D.3; W1/W2/D1/PT1; four-field Plan_Context; Stream_Slot sole ownership | **PASS** |

Graft did the hard thing: it **imported product honesty** (matrix, M6, E0, eng≠product) and **operational density** (close SM, POD footnote, steal-own, apply switch) without reopening dual Loop worlds, fat public context, app-facing seal SM, or Session_Wire.

---

## Rule scores (8)

| # | Rule | Verdict | Score | Note |
|---|------|---------|------:|------|
| 1 | **Data-oriented over interface-oriented** | **PASS** | **9.4** | Ownership cake is data: `Stream_Slot[]` + gen, exclusive `H1_Framer \| H2_Engine` raw union, `Conn_Pt_Ring` POD, `Seal_Unit` queue + `rr_cursor`, CT[2] + `Seal_SM`, orthogonal `Conn_Caps` bit_set, live `Slot_Flow` re-read. Cipher opacity is module boundary (`Conn_Cipher_Engine`), not Provider sermon. **Session write path is a private `switch` on `framer.kind`** — not a product vtable. Residual scar: private `Slot_Deferred.pull` proc (host-only; E0.6 bans app registration). Acceptable ghost; do not promote. |
| 2 | **Explicit constraints, private mechanisms** | **PASS** | **9.6** | PART I freezes intent rails only. Public `Plan_Context` is **exactly four fields** (R2 residual closed and held). Mechanisms (Seal_SM, Wait_Flow, Commit_Unit, mem-BIO, seal_q, PT ring) never mint body APIs or stream-id events. Hangup = `.Client_Gone` only. E0.1–E0.8 hard-fail CI so docs cannot re-teach pipe. Best constraint surface A has had. |
| 3 | **No fake abstraction** | **PASS** | **9.4** | `Session_Wire` product dead (D28). No `Conn_Proto` as app architecture. No dual PT surfaces (Law PT1 **must** alias — “may alias” forbidden). `max_write_unit` remains one intentional coalesce, not a synonym cluster. Fused `Commit_Unit` refuses micro-op taxonomy as public truth. Micro scars: `BIO_RX_HOLD_MAX` synonym of `RX_HOLD_CAP`; Exec_Op still lists clear-path laundry beside fused ops — private, but still a teaching surface if docs leak. Not enough for Partial. |
| 4 | **One way for the common path** | **PASS** | **9.5** | Same handler sample, three listens (E0.4). H1 = N=1 same types. No public duals (`conn_caps`, second hangup, `body_set_pull`, io.Stream SSE). Law S1: one submit_send owner. **Eng Phase 4 ≠ product Phase 5** + M1–M6 + README honesty kill *social* duals (paper-H2, early SSE-on-H2). Multiplex serial only as labeled engineering — not marketed default. |
| 5 | **Phase without dual-maintenance forever** | **PASS** | **9.5** | **Primary R4 craft test.** Steal facts / own types is tabled and decided (D27). Cherry-pick frame/HPACK/vectors; rewrite storage to slots; refuse forever `server/` import. Phases: App freeze → slot truth → TLS physics → H2 engine → unary eng → **product M1–M6** → polish — one host grown in place. Eng≠product is **honesty staging**, not a second architecture. No vapor package fork. R1/B-class Rule 5 FAIL surface does not reappear in the graft. |
| 6 | **Platform honesty (TLS/H2 cost visible)** | **PASS** | **9.6** | Named pipe POD (§F.1); dual PT+CT high-water; CT×2; RX hold fixed; sendfile truth table; ALPN Phase 2 `http/1.1` only; **hard** firehose CI (peak ≳ 4× HW fails); M1–M6 product bar including **M6 concurrent SSE**; author capability matrix ⏳/✅; close free-order SM (§E.4). Peer numbers footnoted as physics, not package import. kTLS not smuggled as current ZC. Crown-tier honesty. |
| 7 | **proactor-native** | **PASS** | **9.5** | T1: arm only from accept/handshake/CQE paths. No public resume/poll. Effects timing host-owned. Socket send_inflight ∈ {0,1}; seal depth separate via Seal_SM / `seal_n`. Gen check on seal_q dequeue and plan abort (W2). Duplex Law D1 under Multiplex documented as law. Free-order close SM forbids free-under-CQE and resume-into-freed-plan. |
| 8 | **Handmade elegance** | **PASS** | **9.35** | Intent / Constraints / Policy / Mechanism / **Slot** / **Pipe** still the whiteboard cut. App Contract + matrix is the freeze spine authors actually freeze. Laws S1, W1/W2, D1, PT1, D4 fairness-as-type are carvable. PART I / PART II audience split improves *reader* craft without splitting *system* ontology. Type sketch remains implementable without inventing a second architecture. **Density tax:** R4 graft log + B packaging + close SM + matrix make the freeze long — craft of the *system* is WOW; craft of the *document* still carries archaeology. Acceptable at freeze; trim after `APP_CONTRACT.md` lands. |

### Overall Score: **9.45 / 10**

### WOWED: **Yes**

Weighted read: rules 2/5/6 are the R4 story (constraint purity, dual-maint refusal, product honesty). Rule 8 is the only soft edge — elegance of *types* holds; elegance of *doc length* does not improve. Still well above bar.

---

## R3 → R4 craft delta

| Area | R3 A (spotcheck 9.35) | R4 A | Craft effect |
|------|----------------------|------|--------------|
| Plan_Context diet | Four fields | Held | No regress |
| Slot/Pipe + PT1 must-alias + Seal_Unit + 4× HW CI | Spine | **Kept (graft log explicit)** | No dilution |
| Steal vs own | Implicit “not a port” | **Normative §A.4 + D27 + §G** | **+ craft** (Rule 5) |
| Session apply / Session_Wire | Implicit no product | **§H.4 switch + D28 hard ban** | **+ craft** (Rules 1, 3) |
| Product honesty | Strong physics; weaker author calendar vs B | **Matrix + M6 + eng≠product + E0** | **+ craft** (Rules 4, 6) |
| Close SM | Free-order list | **Stream vs conn death SM (§E.4)** | **+ mechanism carve** |
| Doc density | Already high | Higher (graft log + PART I mass) | Nit only |
| Dual-maint / vtable | Absent | **Explicitly refused under graft** | Kill tests pass |

R3 predicted residual: Exec_Op length + doc density. Both remain. Neither is a redesign trigger. The graft **did not** trade A’s handmade compression for B’s lesson mass as *type spine* — types still lead; matrix/E0 are freeze gates, not a second ontology.

---

## What still WOW’s (and what newly does)

### Still WOW (spine)

- **Stream_Slot sole exchange; Connection is pipe** — H1 = N=1 same types.  
- **Law PT1 must-alias** — single PT owner; dual full PT window is anti-pattern + redesign trigger.  
- **Seal_Unit + seal_q + rr_cursor + gen** — fairness as type, not vibes.  
- **Tls_Pipe + Seal_SM + mem-BIO + arm-from-CQE** — platform cost as POD.  
- **Laws W1/W2, D1 duplex, S1 single outbound** — whiteboard-carvable.  
- **Four-field public Plan_Context** — progressive disclosure real.  
- **App Contract as docs-as-API** with same-sample CI.

### Newly WOW / hardened at R4

- **Steal vs own table** — peer facts without dual-maint substrate.  
- **Private session apply switch** — SSE framing without Session_Wire product.  
- **M1–M6 + eng≠product** — multiplex meaning is data (gates), not marketing fog.  
- **E0 social bans** — dual APIs cannot return via examples.  
- **Close SM free-order** — implementers must not invent stream vs conn death.

---

## Residual craft nits (not WOW blockers)

1. **Document density.** PART I + A–R + R4 graft log is long for a forever freeze. System craft WOW; ship APP_CONTRACT / MIDDLEWARE / CAPABILITY_MATRIX as the thin public cut and let PART II be implementer-only (already the intent — police links from README).

2. **`Slot_Deferred.Host_Pull` proc.** One private function-pointer ghost of vapor Body_Source. Marked host-only; E0.6 + NEVER list. Do not let it become `body_set_pull` or appear in godoc samples.

3. **Exec_Op laundry.** Clear-path kinds + `Produce_Window` / `Commit_Unit` / `Wait_Flow` still coexist in one enum sketch. Fusion rule is correct; guard reviews against re-growing Write_Plain + Cipher_Seal + Frame_Data as three hot-path interpreter steps.

4. **`BIO_RX_HOLD_MAX` synonym row.** Harmless alias of `RX_HOLD_CAP`; slightly synonym-cluster adjacent. Prefer one name in freeze POD.

5. **`Message_Proto` + Multiplex dual-label** (R3 spotcheck minor). Framing enum vs cap bit still two labels for “H2-ish.” Orthogonal caps story is right; do not grow a third public proto API.

6. **Fairness weights open.** Equal RR v1 + documented SSE-vs-bulk starvation risk is honest (not fake solved). Polish phase only.

---

## What would push A toward 9.6+

- One-line normative: “hot path cursor advances by flush units; Exec_Op list is clear-H1 legacy + Commit_Unit / Wait_Flow / Produce_Window only.”  
- Drop or single-line alias for `BIO_RX_HOLD_MAX`.  
- After Phase 0 lands, **archive graft log** out of the implementer north-star (keep decisions D27/D28; lose process archaeology).  
- Keep Slot/Pipe / PT1 / Seal_Unit / App Contract exactly as written — do not “improve” with Adapter seams.

---

## Anti-regression checklist (R4 freeze police)

Reject PRs / doc edits that reintroduce:

| Forbidden | Why |
|-----------|-----|
| `Session_Wire` / Adapter / public Provider | vtable soup |
| Forever vapor `server/` package as architecture | dual-maint |
| Dual full PT window (Tls_Pipe + Conn_Pt_Ring without alias) | PT1 / peak-mem |
| Fat public Plan_Context (ring/SQE/caps/proto) | fake progressive disclosure |
| Dual Loop vs H2-only slot type | dual worlds |
| App-facing seal SM / Exec_Op tutorials | mechanism as intent |
| README “supports HTTP/2” before M1–M6 | paper multiplex |
| Host_Pull / http/debug / sid in examples | social dual |
| Public resume / second hangup / stream id API | proactor + identity break |

---

## Scoreboard (compact)

| Plan | R1 | R2 | R3 | R4 | R5 | R6 | R7 | R8 | Overall | WOWED |
|------|----|----|----|----|----|----|----|----|---------|-------|
| **A R2** | Pass | Pass | Pass* | Pass | Pass | Pass | Pass | Pass | **9.25** | Yes |
| **A R3** | Pass | Pass | Pass | Pass | Pass | Pass | Pass | Pass | **9.35** | Yes |
| **A R4** | **Pass** | **Pass** | **Pass** | **Pass** | **Pass** | **Pass** | **Pass** | **Pass** | **9.45** | **Yes** |

\*R2 Rule 3 was the closed Partial→Pass on fake abstraction; held through R4.

| Rule | R3 lean | R4 | Δ driver |
|------|---------|-----|----------|
| 1 Data-oriented | Pass | **9.4** | Private apply switch explicit; Host_Pull ghost unchanged |
| 2 Constraints private | Pass | **9.6** | Four-field + PART I / E0 held hard |
| 3 No fake abstraction | Pass | **9.4** | Session_Wire ban + PT must-alias; micro synonym nits |
| 4 One way | Pass | **9.5** | Eng≠product + matrix social freeze |
| 5 No dual-maint forever | Pass | **9.5** | Steal/own table + D27 graft-safe |
| 6 Platform honesty | Pass | **9.6** | M6 + matrix + 4× HW + close SM |
| 7 proactor-native | Pass | **9.5** | Close SM free-order; T1 held |
| 8 Handmade elegance | Pass | **9.35** | Types elegant; doc denser |

---

## Final judgment

**Plan A R4 clears the handmade bar at 9.45 — WOWED.**

The R4 graft was the dangerous move (absorb B residuals into A). It **did not** reintroduce dual-maint forever, vtable soup, Session_Wire product, dual Loop worlds, or fat public context. Steal-vs-own is a real decision with refusal language. Session apply is a private switch. A’s harder bulk law (must-alias PT, 4× firehose CI, typed Seal_Unit, allocator table) remains undiluted.

Ship this freeze spine. Further craft work is **hygiene** (Exec_Op teaching surface, synonym POD, doc trim after APP_CONTRACT lands) — not redesign. Lose WOW only by reopening the anti-regression table above.

---

*Critic: handmade / data-oriented / proactor house rules. Bar WOW ≥ 9. Plan A R4 only. Graft integrity verified. No participation trophies — the types earned it.*
