# Critic R4 — USER ERGONOMICS (Plan A graft only)

**Lens:** few concepts · zero footguns on the common path · progressive disclosure · delightful defaults · honesty in time  
**Bar:** WOW ≥ 9 only. Willing to re-score after R3 residual grafts; not willing to grant WOW on graft-log claims without plan-text proof.  
**Scope:** **Plan A R4 only** (`plan-a.md` status: *R4 graft — B residual wins absorbed; spine remains A*).  
**Prior:** `critic-ergonomics-r3.md` (A **9.2** WOW · B **9.3** WOW · synthesis **9.5**)  
**Focus this round:** Did the graft absorb B’s ergonomics edge (matrix · PART I · E0 · honesty · dual-API social bans · M6 product calendar) **without** diluting A’s App Contract density, four-field `Plan_Context`, or dual-API kill tables?

---

## Score

| Plan | R1 | R2 | R3 | R4 | Δ R3→R4 | Notes |
|------|---:|---:|---:|---:|--------:|-------|
| **A** | 8.0 | 8.8 | 9.2 | **9.45** | **+0.25** | B residual packaging landed in PART I; App Contract still sole public story; four-field context held; dual-API kills + social bans greppable; eng≠product H2 author-visible. Residual is export discipline + one matrix cell wording, not dual APIs. |

**Primary freeze score (user ergonomics, single document):** **9.45 — WOWED.**  
**vs R3 B (9.3):** A R4 **meets and slightly exceeds** B’s author-facing package because it now carries B’s matrix/E0/PART split **and** keeps A’s densest contract + three-listen freeze culture.  
**vs R3 synthesis (9.5):** **within noise** of the ideal merge for ergonomics packaging; remaining −0.05 is craft polish (matrix eng/product cell, monofile discipline), not missing freeze law.

---

## WOWED

| Plan | WOWED | Why |
|------|:-----:|-----|
| **A R4** | **Yes** | R3’s only A demerits vs B were packaging and social honesty tools (matrix, PART I stop, E0 numbered gates, README rules, M6 calendar). Those are **in PART I as law**, not appendix vibes. R2/R3 dual-API kills and Plan_Context diet remain closed. Common path is still “today’s clear-H1 sample · three event words · no `#if`.” Progressive disclosure no longer sells ring free as a constraint **and** no longer sells curl-green as “supports H2.” |

**WOW bar met.** Further ergonomics redesign is not justified. Remaining work is **PR0 export** (`APP_CONTRACT.md`, matrix, CI sample, example lint) and policing — not ontology.

---

## Verification — R3 residual graft must be present

R3 said B edged A (+0.1) on author UX via matrix + PART I + E0 + marketing rules; synthesis asked to absorb those into A. Hard check against `plan-a.md` text:

| R3 residual / B edge | Demand | R4 locus | Present undiluted? |
|----------------------|--------|----------|--------------------|
| **PART I / PART II** | Authors stop after contract + matrix | Front “Who reads what”; `# PART I` / `# PART II`; “App authors: you can stop here” | **Yes** |
| **Author capability matrix** | ⏳/✅ × Clear/TLS H1/TLS H2 × oneshot/large/SSE/WS/concurrent/README | PART I “Author capability matrix”; E0.3; D24 | **Yes** |
| **E0.1–E0.8** | Numbered merge blockers | PART I freeze table; Phase 0 §K; D20; PR0 | **Yes** |
| **README honesty rules 1–4** | No paper-H2; no early SSE-on-H2; listen options; curl≠product | PART I honesty rules; Phase 4 Forbidden; anti-patterns; D25 | **Yes** |
| **M6 + product bar M1–M6** | Concurrent SSE on H2 slots before author “supports H2” | Matrix rows; §J M6; Phase 5; H.6; D16 | **Yes** |
| **Eng ≠ product H2** | Unary eng not marketed | Phase 4 “engineering milestone only”; honesty rule 4; PR8 vs PR9; D11/D24 | **Yes** |
| **Expanded NEVER** | Host_Pull from app; stream_* second product; third intent rail | App Contract NEVER block; B.2; D30; F.4 + E0.6 | **Yes** |
| **Soft 503 note** | Temporary host behavior in contract | PART I “Temporary host behavior”; D29 | **Yes** |
| **Dual-API social ban** | Examples = types for ferocity | E0.5–E0.7; dual-API social ban paragraph; anti-patterns | **Yes** |

### A spine that must not dilute (ergonomics)

| A R3 strength | R4 locus | Held? |
|---------------|----------|-------|
| App Contract sole public story (dense one-pager export) | Opens PART I; “only public story”; E0.1 ≤1 page copy rules | **Yes** |
| Four-field `Plan_Context` only | C.2 + type sketch + D2; ring/SQE/iovecs host-private | **Yes** |
| Dual-API kill tables | NEVER + B.2 + anti-patterns + failure triggers | **Yes — stronger** |
| Hangup / Writable unity | Hangup law; events list; D5 | **Yes** |
| Three-word mental model | Intent / Exchange / Backpressure | **Yes** |
| Middleware card | PART I may/must-not | **Yes** |
| Same-handler / zero `#if` freeze culture | E0.4; B.1 sample; Q success | **Yes** (progressive phase jobs, zero-branch law intact) |

**Graft integrity: PASS.** B residual wins are **in-law in PART I**. A’s public-story density is not diluted into implementer epic as the first page.

---

## R3 → R4 residual scorecard (ergonomics)

### What R3 required for A ≥ synthesis packaging

| Residual | R3 A | R4 A | Verdict |
|----------|------|------|---------|
| **1. Capability matrix as author product** | Phase honesty table only; B had full ⏳/✅ | Full matrix + E0.3 artifact gate | **Closed** |
| **2. PART I stop line** | Contract first; long body after (soft split) | Hard PART I/II + who-reads-what + stop sentence | **Closed** |
| **3. E0 numbered freeze** | Phase 0 checklist (good) | E0.1–E0.8 merge blockers (B-style reviewability) | **Closed** |
| **4. README / marketing honesty** | Multiplex gates; less author calendar | Honesty rules 1–4 + matrix README row + Phase 4 Forbidden | **Closed** |
| **5. SSE-on-H2 product honesty** | First marketed H2 included sessions (A Phase 3 then) | Matrix ⏳ until Phase 5 + M6; eng Phase 4 forbidden as product | **Closed (schedule = B honesty; still one API)** |
| **6. Dual-API social bans** | NEVER + anti-patterns | + E0.5–E0.7 + Host_Pull/godoc ban + dual-API social ban shout | **Closed (sharper)** |
| **7. Plan_Context thinness** | Four fields (R3 closed) | Four fields held; removed list explicit | **Held** |
| **8. App Contract thinness / sole story** | Dense open | Still sole story; export path ≤1 page for contract text; matrix may be sibling file | **Held** |

### R1 Fatals / R2 Majors (still closed?)

| Item | A R4 |
|------|------|
| No App Contract | Closed · sole story · PART I |
| `_sid` / Stream_Reset / public pull / io.Stream | Never · E0 bans |
| Fat Plan_Context / escape caps | Four fields only · debug only |
| Middleware contract | Card + §I |
| Hangup / Writable unity | Law |
| Dual Loop / dual hangup / dual long-lived | Anti-patterns + NEVER |

**No R1 Fatal reopened. No R2 Major reopened. No R3 design residual left open for packaging.**

---

## Strengths (R4)

1. **The freeze product is PART I.** Who-reads-what, App Contract, matrix, honesty rules, E0 — an author can stop and be safe. Implementer physics no longer competes for first-page attention.
2. **Capability matrix is WOW-grade time UX.** Clear / TLS H1 / TLS H2 × oneshot / large / SSE / WS / concurrent / README — with ⏳/✅ and phase numbers. Turns “correct on H2” from slogan into a calendar. Matches R3 B’s edge inside A’s spine.
3. **Eng ≠ product is author-visible.** Phase 4 curl green is labeled engineering and **forbidden** as README “supports HTTP/2.” Product bar = M1–M6 including concurrent SSE. Paper-H2 and early SSE-on-H2 are social duals the types alone cannot kill; honesty rules kill them.
4. **App Contract density held.** ONESHOT / LONG-LIVED / ADVANCED / NEVER; three words; hangup law; unary bytes law; long-lived law; identity law; soft-503 note; middleware card. E0.1 still demands ≤1-page export without seal SM / Exec_Op / Provider / Tls_Pipe.
5. **Plan_Context progressive disclosure is pure.** Exactly four public fields. Ring free, SQE budget, iovecs, fixed_files, caps, proto stay host-private. Correctness never requires opening advanced surface.
6. **Dual-API kill surface is greppable and social.** NEVER list expanded (Host_Pull, stream_* second product, third intent rail). B.2 table. E0.5–E0.7 example bans. Dual-API social ban paragraph: “freeze is theater if examples reintroduce duals.”
7. **Common path remains delightful.** B.1 sample is today’s clear-H1 vocabulary across TLS/H2. Events: Start | Timer | External | Client_Gone | Idle_Timeout | Writable. No SSL\*, no stream id, no resume, no pull registration.
8. **SSE-on-H2 is one product, not a second package.** Same `sse_start` / Effects / `.Client_Gone`; readiness is Phase 5 / M6. H.6: “Do not ship a separate H2 session package.” Source identity preserved for listen flips.
9. **Two rails only, forever.** Commands | Effects. Host windowing is mechanism. Progressive `stream_*` refused as long-lived product. Unary never grows a third rail.
10. **CI as ergonomics process.** Same handler sample; zero protocol `#if`; fail on stream-id samples; plan policy tables (E0.8). Freeze is not culture — it is merge-blocker checklist + PR0.

---

## Dual-API audit (explicit)

| Temptation | A R4 | Pass? |
|------------|------|-------|
| Second hangup event | `.Client_Gone` only | **Yes** |
| Stream id on Response/Session | Never; gen only; E0.7 | **Yes** |
| Public caps / Message_Proto / Conn_Proto | Host-private / debug; NEVER + E0.5 | **Yes** |
| `body_set_pull` / app Host_Pull | Host windowing only; NEVER + E0.6 + no third rail | **Yes** |
| `io.Stream` long-lived | Refuse in freeze + anti-pattern | **Yes** |
| Public resume / poll | Never | **Yes** |
| Body modes = writev/sendfile/SSL_write | Never | **Yes** |
| Fat Plan_Context (ring/SQE/iovecs) | Four fields only | **Yes** |
| Silent H2 serial as “H2” | Phase 4 eng only; M1–M6 before product language | **Yes** |
| SSE-on-H2 as later dual product API | Same API; Phase 5 readiness; not second package | **Yes** |
| Progressive `stream_*` second long-lived path | NEVER + D30 | **Yes** |
| Docs teaching Exec_Op/Tls_Pipe as handler API | Forbidden; PART I stop; E0.1 | **Yes** |
| README / examples reintroduce duals | Honesty 1–4 + E0.5–E0.7 + social ban | **Yes** |

**Hangup unity:** closed.  
**Pull/stream duals:** closed public + sample ban.  
**Escape hatches:** closed app-facing.  
**Plan_Context dual class:** closed.  
**Marketing duals:** closed by matrix + Phase 4 Forbidden + honesty rules.

---

## Issues

### Fatal

**None.** Dual-API ship-blockers stay dead. Plan_Context fat stays dead. App Contract is sole public story. Matrix + E0 + eng≠product honesty are explicit. Graft did not reopen R1/R2 poisons.

### Major

**None that block WOW or freeze redesign.**

If harsh process bar must name one **Major-class process** item (not design):

1. **Neither `APP_CONTRACT.md` nor CAPABILITY_MATRIX nor example lint is the repo artifact yet.**  
   Ergonomics freezes become *true* when Phase 0 / E0 exits land. Plan specifies that correctly.  
   **Do not re-score design for this.** Treat as PR0 merge blocker for *implementation*, already specified.

### Minor (residuals — craft, not duals)

1. **Matrix oneshot TLS H2 cell mixes eng milestone with product readiness.**  
   Row: “✅ Phase 4 (unary eng); **multiplex concurrent** ✅ Phase 5.” Honesty rules and Phase 4 Forbidden correctly ban README “supports HTTP/2,” but a hurried author may read oneshot ✅ as “H2 product ready.” Prefer wording that marks Phase 4 oneshot as **eng / optional listen** until Phase 5, or a footnote “not marketable H2.” Score impact already in −0.05 vs ideal synthesis.

2. **`Host_Pull` still appears in private type sketches (F.4).**  
   Commented host-only; NEVER + E0.6 mitigate. Residual social risk if a tutorial author copies the struct. Keep Host_Pull out of any public godoc path forever (same R3 B minor).

3. **PART I still lives in one long monofile with PART II.**  
   Stop line is hard and good; still relies on discipline not to scroll. Ship E0.1/E0.2/E0.3 as separate short files at PR0 — plan already says so.

4. **E0.4 is progressive (“later jobs add TLS/H2”) vs R3 A’s three-listen shout.**  
   Correct for phased landing; zero `#if` law is intact. When Phase 5 lands, same-handler under all three must be CI-hard again (Q success already lists it).

5. **Progressive `stream_*` residual is wording; tree work remains.**  
   NEVER + long-lived law say fold or deprecate in APP_CONTRACT. Confirm at PR0 against existing proactr progressive APIs.

6. **Admission assert → soft 503 remains temporary host behavior.**  
   Named in App Contract (good). Keep one sentence forever so load-test footguns are not silent API changes.

7. **Debug package temptation remains.** E0.5 bans examples importing `http/debug`. CI lint is the real close.

---

## Axis scorecard (ergonomics only, A R4)

| Axis | Grade | Note |
|------|:-----:|------|
| App Contract as sole story | **9.5** | Opens PART I; export ≤1 page; implementer epics banned from tutorials |
| Plan_Context progressive disclosure | **9.6** | Four semantic fields; host meters private; correctness never requires open |
| Dual-API kill completeness | **9.5** | NEVER + B.2 + E0 social bans + anti-patterns |
| Hangup / Writable unity | **9.6** | Law; single event; codes → logs only |
| Pull / stream duals | **9.5** | Host-only windowing; no app registration; Effects for long-lived |
| Escape hatches | **9.4** | Caps/proto debug-only; no public resume/SSL/stream id |
| SSE-on-H2 phase honesty | **9.4** | Matrix ⏳ → Phase 5 + M6; same API; eng≠product |
| Multiplex / marketing honesty | **9.5** | Honesty 1–4 + Phase 4 Forbidden + M1–M6 product bar |
| Docs/CI freeze gates | **9.5** | E0.1–E0.8 numbered; dual-API social ban |
| Author discoverability | **9.4** | Who-reads-what + PART I stop (monofile residual −0.05) |
| Mental model load (apps) | **9.5** | Intent / Exchange / Backpressure; Request/Response/Session/Effects |
| Capability matrix | **9.4** | Full ⏳/✅; oneshot Phase 4 cell slight muddle |

**Mean (axis):** **~9.45** — matches headline score.

---

## Score reconciliation (R3 → R4)

| R3 gap for A vs B / synthesis | R3 effect | R4 status | R4 pts |
|-------------------------------|----------:|-----------|-------:|
| No full author capability matrix | −0.08 | Matrix in PART I + E0.3 | **0** |
| Soft PART I stop / book structure | −0.05 | Who-reads-what + PART headers + stop sentence | **0** |
| E0 numbered social freeze weaker than B | −0.05 | E0.1–E0.8 merge blockers | **0** |
| Marketing / paper-H2 honesty tools thinner | −0.05 | Honesty 1–4 + Phase 4 Forbidden + anti-patterns | **0** |
| M6 calendar as product readiness | −0.02 | M6 + matrix concurrent SSE + Phase 5 | **0** |
| *Already closed at R3* (context diet, dual kills, hangup, contract density) | 0 | Held / strengthened | **0** |
| Matrix eng/product oneshot cell | — | Minor residual | **−0.05** |
| Monofile + PR0 not landed (process; partial) | — | Stop line good; artifacts pending | **−0.00 design** |
| **Base** | 10 | | 10 |
| **Result** | **9.2** | | **9.45** |

*(Net +0.25: B packaging residual wins absorbed without reopening dual APIs or fattening Plan_Context.)*

---

## Comparative note (only as graft proof)

R3 B scored **9.3** on author ergonomics packaging. R4 does not re-score Plan B. For freeze selection:

| Dimension | R3 A | R3 B | **A R4** |
|-----------|------|------|----------|
| App Contract density | Edge A | Good | **Held A density + B book structure** |
| Capability matrix | Weaker | Edge B | **Absorbed** |
| E0 social freeze | Checklist | Numbered edge B | **Absorbed** |
| Plan_Context thinness | Tie (four fields) | Tie | **Held** |
| Dual-API kills | Excellent | + social bans | **Absorbed social bans** |
| SSE-on-H2 honesty | Earlier parity | Clearer calendar | **Calendar (B honesty schedule)** |

**Verdict:** Graft achieved the R3 synthesis ask for **user ergonomics packaging**. Prefer **A R4 as the single freeze document** for authors + implementers. Do not keep a dual-plan author story.

---

## What WOW already is (no further design work)

R3 residual list for ergonomics is **done on paper**:

1. ✅ Public Plan_Context = four fields.  
2. ✅ App Contract sole public story + PART I stop.  
3. ✅ Author capability matrix ⏳/✅.  
4. ✅ SSE-on-H2 honesty (Phase 5 / M6; eng Phase 4 not product).  
5. ✅ Dual-API kills held + E0 sample bans.  
6. ✅ Phase 0 / E0 docs+CI freeze as hard gate.  
7. ✅ README honesty rules (paper-H2 / early SSE / listen options / curl≠product).

### Remaining for *shipping* WOW (implementation PR0 — not R5 redesign)

- [ ] Land `docs/APP_CONTRACT.md` ≤1 page (no Exec_Op, no seal SM, no Provider, no Tls_Pipe).  
- [ ] Land `docs/MIDDLEWARE_CONTRACT.md`.  
- [ ] Land `docs/CAPABILITY_MATRIX.md` (or table inside APP_CONTRACT per E0.3).  
- [ ] CI: same handler sample; zero protocol `#if` / stream-id; grow TLS/H2 jobs as phases land.  
- [ ] Lint: `examples/` no `http/debug`, no Host_Pull registration, no sid.  
- [ ] README: TLS/H2 are listen options; no “supports HTTP/2” before M1–M6.  
- [ ] Optional polish: clarify oneshot TLS H2 matrix cell as eng-not-market until Phase 5.  
- [ ] Confirm progressive `stream_*` deprecation/fold in tree when APP_CONTRACT lands.

---

## Ruthless one-liners

- **A R4:** You absorbed B’s author calendar without reopening dual APIs or fattening constraints. The App Contract is still the product; the matrix is the clock.  
- **Graft truth:** PART I is no longer “A denser, B clearer” — it is one freeze surface.  
- **Score truth:** R1 surgery → R2 dual kills → R3 diet + honesty → **R4 packaging synthesis on A’s spine.** Stop redesigning. Start Phase 0.  
- **Lose WOW only by:** lying in README, teaching pull from app code, or putting ring/SQE back on `Plan_Context`.

---

## Close-out vs prior rounds

| Plan | R1 Fatals open | R2 Majors open | R3 packaging residuals open | R4 Majors open | Score | WOWED |
|------|----------------|----------------|-----------------------------|----------------|------:|:-----:|
| **A** | 0 | Plan_Context residual fat (closed R3) | Matrix / PART I / E0 / honesty (open vs B) | **0** | **9.45** | **Yes** |

**Ship-as-freeze for user ergonomics?** **Yes — Plan A R4.**  
**Primary freeze score:** **A 9.45.**  
**Further ergonomics critic rounds:** not justified unless PR0 artifacts violate the freeze (example duals, fat Plan_Context regression, README H2 lies, matrix contradiction).

---

*Critic: user ergonomics R4 · dual-tls-h2 run 4e65dac9 · Plan A graft only · bar WOW ≥9 · **WOWED yes 9.45** · matrix/PART I/E0/honesty grafted · App Contract sole story · four-field Plan_Context held · duals dead · stop redesigning · start Phase 0*
