# Handmade R4 scratch — Plan A only

**Bar:** WOW ≥ 9  
**Baseline craft:** R2 A 9.25 · R3 spotcheck A **9.35** · B R3 9.2  
**Subject:** `plan-a.md` R4 graft (B residuals into A spine)

---

## Mandate checks (user)

| Check | Where | Result |
|-------|-------|--------|
| Graft ≠ vapor dual-maint forever | §A.4, §G engine port, D27, anti-pattern “Forever dual-maint…”, Phase 3 exit “OWNERS: one tree” | **Clean** |
| Graft ≠ vtable soup | §A.1 “No Adapter/Session_Wire/Provider product”; §H.4 private switch; D28; graft log “Not reintroduced: Session_Wire product type” | **Clean** |
| Steal-vs-own | §A.4 table + §G bullets + D27 | **Normative, freeze-grade** |
| Session apply private switch | §H.4 `session_apply_write` sketch on `framer.kind` | **Present** |
| No Session_Wire product | NEVER list, anti-patterns, D28, graft log | **Hard ban** |

---

## Eight rules (quick)

1. **Data-oriented** — Slot/Pipe POD, Conn_Pt_Ring, Seal_Unit[], CT[2], caps bit_set, exclusive framer raw_union. Apply path is switch not vtable product. Host_Pull proc = one private ghost (same R2/B scar).
2. **Constraints private mechs** — four-field PC; PART I bans seal/Exec_Op/Tls_Pipe in app docs; E0 hard.
3. **No fake abstraction** — Session_Wire dead; Conn_Proto not app architecture; dual PT forbidden (PT1 must-alias). BIO_RX_HOLD_MAX synonym of RX_HOLD_CAP = micro scar.
4. **One way** — same sample × three listens; eng Phase 4 ≠ product Phase 5; no dual body APIs.
5. **No dual-maint forever** — steal facts / own types; refuse `server/` fork; one host grown in place. Eng≠product is honesty, not package fork.
6. **Platform honesty** — POD table, dual HW, 4× HW CI, M1–M6, matrix ⏳/✅, ALPN Phase 2 http/1.1 only.
7. **proactor-native** — T1 arm-from-CQE; no public resume; close SM free-order; Seal_SM not bare bool alone.
8. **Handmade elegance** — Intent/Constraints/Policy/Mechanism/Slot/Pipe; PART I/II audience cut; graft log transparent. Doc denser than ideal.

---

## Score draft

| Rule | Pass? | Numeric lean |
|------|-------|--------------|
| 1 | Pass | 9.4 |
| 2 | Pass | 9.6 |
| 3 | Pass | 9.4 |
| 4 | Pass | 9.5 |
| 5 | Pass | 9.5 |
| 6 | Pass | 9.6 |
| 7 | Pass | 9.5 |
| 8 | Pass | 9.35 |
| **Overall** | | **~9.45** |

**WOWED?** Yes (≥9). Δ vs R3: **+0.1** (graft closed dual-maint/vtable attack surface without diluting A spine).

---

## Residual nits (not WOW blockers)

1. Doc density (App Contract + A–R + R4 graft log + matrix).
2. Slot_Deferred.Host_Pull proc pointer (private; E0.6 bans app use).
3. Exec_Op clear-path laundry still listed beside Commit_Unit.
4. BIO_RX_HOLD_MAX as synonym of RX_HOLD_CAP.
5. Message_Proto + Multiplex dual-label framing (spotcheck R3 minor).
6. Fairness weights still polish-open (honest, not fake).

## Would push 9.6+

- Collapse Exec_Op public-looking laundry to “clear legacy + Commit_Unit/Wait_Flow/Produce_Window only” one-liner normative.
- Drop BIO_RX_HOLD_MAX synonym row or mark alias-only once.
- Optional: shrink graft log after freeze lands in APP_CONTRACT (process archaeology ≠ forever spine).

## Kill conditions for this pass (none tripped)

- Session_Wire product type reintroduced → **not present**
- Forever dual-maint vapor `server/` as architecture → **refused**
- Dual PT without must-alias → **must-alias kept**
- Fat public Plan_Context / dual Loop → **not reopened**
- Provider/Adapter public thesis → **module-private only**
