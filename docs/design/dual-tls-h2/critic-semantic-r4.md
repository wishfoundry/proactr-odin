# Critic — SEMANTIC COMPRESSION (r4 · Plan A only)

**Lens:** maximum power per concept · orthogonal vocabulary · no synonym types · no parallel worlds · few true primitives that compose · App Contract thin · **graft must not inflate**  
**Bar:** WOW ≥ 9  
**Artifact:** Plan A R4 graft (`plan-a.md`)  
**Baselines:** r2 A **9.15** WOWED · r3 spotcheck A **9.3** WOWED  
**Question of the round:** Did the B residual graft (PART I/II, matrix, M6, close SM, steal/own, …) **bloat or compress**?

Scratch inventory: `critic-semantic-r4-scratch.md`

---

## Verdict (one line)

**Graft compressed. Type ontology held; product/author meaning got sharper. No new runtime duals.**  
**Plan A R4 ≈ 9.38 · WOWED.** Residuals are private dual-labels and teaching surface — not ontology forks, not graft scars that reopen r1 fatals.

---

## 0. Graft inflation audit (the only R4 question)

| Graft item (from B) | New type / peer role? | Effect on compression |
|---------------------|------------------------|------------------------|
| **PART I / PART II** | **No** | **Compresses reader ontology** — authors stop after contract + matrix; Slot/Pipe/Seal never load as public concepts |
| **Capability matrix ⏳/✅** | **No** | Readiness as **data**, not slogan. Compresses “when is the invariant true?” |
| **E0.1–E0.8** | **No** | Social dual-API reentry closed as **CI gates**, not new surface |
| **README honesty rules** | **No** | Collapses paper-H2 and early SSE-on-H2 into forbid law |
| **M6 + product bar = M1–M6** | **No** | **Folds** SSE-on-H2 into product meaning of H2 — prevents a second long-lived product later |
| **Eng Phase 4 ≠ product Phase 5** | **No** | Separates curl-green from “supports HTTP/2” without a second handler API |
| **Close SM §E.4** | Steps / free-order, **not** new exchange type | **One teardown ontology** (stream vs conn); kills invent-your-own free path |
| **Steal vs own §A.4** | Process boundary, **not** runtime type | **Compresses dual-maint substrate** (refuse forever vapor `server/` fork) |
| **Private `framer.kind` switch** | Explicit **refuse** of Session_Wire product | Prevents vtable dual for H1/H2 write path |
| **Host_Pull NEVER / third rail ban** | Ban, not type | Closes latent third intent rail at contract + examples |
| **Soft-503 host note** | No | Keeps admission assert out of app event space |
| **Peer POD footnote** | No | Numbers as physics footnotes — not package import ontology |

**Score the graft as a unit:**

| Check | Pass? |
|-------|-------|
| Did graft invent a peer role next to Intent / Slot / Pipe? | **No** |
| Did graft reintroduce synonym caps / fat Plan_Context / stream-id API / dual hangup? | **No** |
| Did graft create a second long-lived product or third intent rail? | **No** — M6 + NEVER bans do the opposite |
| Did doc *apparatus* grow (matrix, E0, M1–M6, honesty, steal/own)? | **Yes** — by design |
| Is that apparatus **anti-inflation** (gates that forbid duals) or **synonym mass**? | **Anti-inflation** |

**Conclusion:** R4 graft is **compression of product honesty and reader cut**, not concept inflation. Wordcount up (~1231 lines); **load-bearing primitives unchanged** (~3 app words + ~8 implementer cores). That is the correct trade for a freeze book.

---

## 1. Target primitive budget (unchanged bar)

| Role | Job | Public? |
|------|-----|---------|
| **Intent** | cmds \| effects — exactly two rails | Yes |
| **Constraints** | thin `Plan_Context` (four semantic fields) | Yes, optional |
| **Policy** | pure `plan_body` | Host |
| **Mechanism** | produce → seal/frame → one submit | Private |
| **Slot** | sole exchange ownership | Host name; apps say exchange |
| **Pipe** | socket + cipher? + framer + one send schedule | Host |
| **Event** | unified `Session_Event` only | Yes |

**Test:** Does TLS or H2 invent a *peer* role, a *synonym type*, or a *second home* for slot/intent? If yes → fail. If only fills caps / private physics / phase honesty → pass.

**R4 result:** **Pass.** Graft filled honesty and free-order density only.

---

## 2. Concept inventory (R4)

### Public (authors — PART I)

| Name | Necessary? | R4 note |
|------|------------|---------|
| `Response_Cmd` / body_* / `respond` | **Yes** | Sole unary rail |
| `Effects` / `sse_start` / `ws_start` | **Yes** | Sole long-lived rail |
| `Session_Event` (incl. `.Client_Gone`, `.Writable`) | **Yes** | One hangup; one backpressure |
| `Plan_Context` (4 fields) | **Yes optional** | **Skeletal** — r2 m3 closed and held |
| Middleware card (may / must not) | **Yes** | Cmd rewrite only |
| Capability matrix ⏳/✅ | **Yes as freeze data** | **Not a type** — calendar honesty |
| E0.1–E0.8 | **Yes as CI law** | Dual-API social ban |

### Private (implementers — PART II)

| Name | Necessary? | R4 note |
|------|------------|---------|
| `Stream_Slot` | **Yes — core** | Sole storage; H1 = N=1 |
| `Connection` (pipe) | **Yes — core** | No session/plan storage |
| `Session` (gen, `_slot`) | **Yes** | ABA; never frame_id |
| `Conn_Caps` (4 bits) | **Yes private** | Orthogonal; no synonym cluster |
| `Message_Proto` | **Borderline** | Residual dual-label w/ Multiplex / framer.kind (m1) |
| `Plan_Host` | **Yes** | Staging meters stay off public PC |
| `Tls_Pipe` + `Seal_SM` + CT×2 | **Yes private** | Normative L2; SSL* sealed |
| Close SM §E.4 | **Yes as law** | Stream vs conn free-order — **graft density, not new type** |
| `Conn_Pt_Ring` + Law PT1 must-alias | **Yes** | Single PT owner — A spine kept |
| `Seal_Unit` + `seal_q` + `rr_cursor` + gen | **Yes** | Fairness as type |
| `Wire_Conn` / `Wire_Slot` | **Yes** | Real ownership split |
| `Slot_Flow` + Laws W1/W2 | **Yes when H2** | Live windows; plan abort |
| Exclusive framer raw-union | **Yes** | Not Frame_State kitchen sink |
| `Slot_Deferred` / private Host_Pull | **Yes private** | Third rail **refused** for apps |
| `Exec_Op_Kind` | **Role yes; set fat** | Residual laundry (m2) |
| Pipe POD constants | **Yes private** | Peer-measured footnotes OK |
| Steal vs own | **Process only** | Maintenance compression |
| M1–M6 product gates | **Phase law** | Meaning of “H2 ready” — not types |

**Load-bearing public concepts:** Intent · Exchange · Backpressure · (optional thin Plan_Context).  
**Load-bearing implementer concepts:** Slot · Pipe · Framer · Cipher · Wire_Conn · live flow · (PT ring as pipe admission).  
**Named design objects:** ~18–20; **~8–10 load-bearing** — **same order as r2/r3**. Graft added **zero** peer runtime types.

---

## 3. Scorecard (Plan A only)

| Axis | r2 | r3 spot | **r4** | Note |
|------|----|---------|--------|------|
| Primitive budget | 9.3 | ~9.4 | **9.4** | Two rails + four-field PC locked; matrix is data not primitive |
| Orthogonal vocabulary | 9.0 | ~9.1 | **9.15** | Four caps held; Host_Pull ban tightens third-rail edge |
| No parallel H1/H2 worlds | 9.7 | 9.7 | **9.7** | Phase 1 dual-write still hard; framer exclusive; no Session_Wire |
| No synonym types | 8.7 | 8.7 | **8.75** | m1 still open; BIO_RX_HOLD_MAX alias is a nit scar |
| Mechanism privacy | 9.2 | 9.25 | **9.3** | Commit_Unit + windows public contract; SSL* sealed; Exec_Op still zoo |
| Composability TLS×H2 | 9.4 | 9.4 | **9.45** | Same sample CI; M6 same callbacks — no second session package |
| App Contract thin / discoverable | 9.1 | 9.3 | **9.4** | PART I stop + matrix + E0 = best author cut A has shipped |
| Phase honesty | 9.3 | 9.35 | **9.5** | Eng≠product H2; M6; honesty rules; curl≠badge |
| **Overall compression** | **9.15** | **9.3** | **9.38** | **WOWED** |

**WOWED?** **Yes (≥9).** R4 is a **net lift** on honesty and reader cut with **no dilution** of Slot/Pipe/W1/PT1 spine.

---

## 4. What WOW’s (R4)

1. **Still the cut:** `Stream_Slot` sole ownership; `Connection` = pipe; H1 = N=1 same types. Graft did not touch this.  
2. **Four-field public `Plan_Context`** — r2’s largest residual remains closed; host meters private.  
3. **Two intent rails only**; hangup one event; Writable unifies buffer + flow.  
4. **Law W1 purity** — live windows never frozen as plan token bucket.  
5. **PART I stop line** — progressive disclosure of the *document*, so authors never learn implementer nouns.  
6. **Capability matrix** — platform honesty as a table authors can trust without reading Tls_Pipe.  
7. **M6 folds long-lived into H2 product meaning** — maximum power per product gate: concurrent unary *and* concurrent SSE before any “supports HTTP/2.”  
8. **Steal facts / own types** — process compression; refuses dual-maint vapor package as architecture.  
9. **Close SM free-order** — one teardown ontology under stream RST vs conn death (gen-checked seal_q, never free under CQE).  
10. **Must-alias Conn_Pt_Ring + 4× HW firehose CI** — A spine retained; single PT owner; no dual-PT regress.

---

## 5. Residuals (harsh minors — none block WOW)

### m1. `Message_Proto` × `Multiplex` × `framer.kind` (private dual-label) — held from r2

Three names for “which framing bag.” Honest engineering (caps = capability bit; framer = exclusive storage; Message_Proto = slot label), but compression culture wants **one primary**: e.g. `framer.kind` is truth; Multiplex derived for planner tables; Message_Proto is alias of framer.kind, not a third authority. **Minor. Not graft-caused.**

### m2. Exec_Op laundry still the private teaching surface — held from r2

```
Write_Slice, Writev, Sendfile, Copy_Into, Patch_CL,
Flush, Wait_Flow, Produce_Window, Commit_Unit
```

Fusion rule for `Commit_Unit` is correct. Prefer a **role table** (Produce / Seal / Submit / Wait_Flow / Flush unit) with clear H1 ops as *instances* under `!Ciphered`, not peer conceptual families. **Minor — private only.** R4 did not absorb B’s role table as primary teaching; also did not make it worse.

### m3. Explicit POD synonym: `BIO_RX_HOLD_MAX` ≡ `RX_HOLD_CAP`

§F.1 lists both with “synonym/alias.” Drop the alias row; keep one name. **Nit.** Graft scar from peer-footnote fidelity — harmless if deleted.

### m4. Pedagogic readiness thrice (doc mass, not type inflation)

| Apparatus | Audience |
|-----------|----------|
| Capability matrix | Authors — what may I write *now* |
| M1–M6 | Product meaning of H2 |
| Phase 0–6 / PR plan | Implementer schedule |

Three calendars for one honesty story. Acceptable freeze-book density; not synonym types. Optional doc edit: matrix cells cite M* gates once, Phase K cites matrix once — avoid re-deriving product meaning in three full tables. **Ignore for score.**

### m5. Thesis stated 3–4×

App three words · A.3 six concepts · §R reminder · spine line. Fine for freeze; not type inflation. Same r2 m4 class.

### m6. Micro-names

`H2_Engine` vs occasional “H2 framer” prose; `Conn_Cipher_Engine` vs `Tls_Pipe.engine`. Pick one private name per bag. **Nit.**

**No Fatals. No Majors. No graft-induced dual homes.**

---

## 6. Did the graft bloat or compress? (final answer)

| Layer | R3 → R4 | Judgment |
|-------|---------|----------|
| **Runtime type ontology** | Held | **No bloat** |
| **Public app surface** | Tighter (Host_Pull NEVER, third rail, soft-503 note) | **Compress** |
| **Product meaning of H2** | M1–M5 → **M1–M6**; eng≠product explicit | **Compress** (fewer ways to lie) |
| **Reader ontology** | PART I stop | **Compress** |
| **Teardown law** | Thin free-order → normative close SM | **Compress free-path invention** (doc density up, dual free paths down) |
| **Maintenance model** | Steal/own table | **Compress dual-maint** |
| **Doc wordcount / sections** | Grew | **Apparatus up** — but anti-dual gates, not synonym types |

**Net: compress.** A freeze that grew pages while **holding** load-bearing count and **sharpening** product honesty is the right R4 move. Concept inflation would have been: new public rails, Session_Wire product, second hangup, fat Plan_Context return, dual Loop, or “M6 as a second session package.” None occurred.

---

## 7. Trajectory

| Round | Overall | WOWED | One-line |
|-------|---------|-------|----------|
| r1 | 8.4 | No | Thesis strong; caps/context denorm |
| r2 | 9.15 | Yes | Caps/context/escape hatches closed |
| r3 spot | 9.3 | Yes | Four-field PC + Seal_Unit/PT ring held |
| **r4** | **9.38** | **Yes** | **B honesty graft absorbed; spine undiluted; type count flat** |

**Ceiling without redesign:** ~9.5 if m1 primary-label + m2 role table + m3 drop alias — PR0 doc/type-sketch polish, not a fifth writer round on ontology.

---

## 8. Freeze recommendation (compression-only)

1. **Freeze Plan A R4 as the semantic spine.** WOW bar clear.  
2. **Do not re-open** Slot/Pipe, two rails, W1, PT1 must-alias, four-field PC, M1–M6 product bar for “more compression.”  
3. **Optional polish (non-blocking):**  
   - Document framer.kind as framing primary; Multiplex derived; Message_Proto alias.  
   - Prefer Produce/Seal/Submit role table in implementer prose; keep Exec_Op as private instances.  
   - Delete `BIO_RX_HOLD_MAX` synonym row.  
4. **Ship discipline, not redesign:** land `APP_CONTRACT.md`, matrix, E0 CI, example bans — that is where WOW is *lost* if duals return socially.

---

## 9. One-line for the board

> **Count concepts.** R4 still has **three app words** and **~eight implementer cores**.  
> **Graft?** Apparatus that *forbids* duals (PART stop, matrix, M6, E0, steal/own, close SM) — **compresses product meaning**, does not invent peer types.  
> **Synonyms?** Private dual-labels only (Message_Proto, BIO_RX alias, Exec_Op laundry).  
> **WOW ≥9:** **Yes — 9.38.** Spine A; honesty from B; no dilution.

**Harsh ranking this critic:** Plan A R4 is **freeze-grade semantic compression**. Further rounds that add more named frameworks without removing duals would be the first real inflation risk — **stop grafting; enforce the freeze in tree.**

---

*Critic: semantic compression R4 · Plan A only · bar WOW ≥9 · **WOWED yes (9.38)** · graft compressed · residuals minor · no redesign*
